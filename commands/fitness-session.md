---
description: Pre-session brief — today's plan, target loads, recent context.
---

Invoke the `fitness-coaching` skill.

Locate the user's data directory via `.fitness-coach.json` (current working directory or any parent). Read `CLAUDE.md`, `memory/MEMORY.md` and each linked memory file, the active `week-{N}-plan.md`, and the tail of `workout-log.md` (last ~3 sessions).

Produce a brief for today's session: which session is scheduled, target loads/volumes, recent fatigue/recovery signals, and anything to watch for. Respect the user's autonomy preference and coaching tone from `memory/coaching-preferences.md`.
